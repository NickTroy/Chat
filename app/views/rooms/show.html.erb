<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= '@room.user' %>
</p>

<p>
  <strong>Title:</strong>
  <%= @room.title %>
</p>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', rooms_path %>
